[package]
name = "linux-process-manager"
version = "0.1.0"
edition = "2024"

[dependencies]
regex = "1"
libc = "0.2"
eframe = "0.27"
egui = "0.27"
chrono = "0.4"
nix = { version = "0.27", features = ["signal", "process"] }
